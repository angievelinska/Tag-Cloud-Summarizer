

This toolbar offers advanced formatting features such as:

3* Insert a new table 
3* Insert a column left of the selected column. 
3* Add column right to the selected column.
3* Select a column
3* Delete selected columns
3* Insert row above the selected row.
3* Add row below the selected row.
3* Select row
3* Delete selected rows
3* Merge cells
3* Split merged cells horizontally and vertically.
3* Split cells horizontally (left icon) and vertically (right icon)
3* Merge tables
3* Set table attributes

All these options are available via menu or context menu too.
To apply them, select the cells, and
Choose formatting options either from

the tool bar,
the menu bar (Format | Table | ... 
or choose Table | ... from the context menu.

For formatting options within table cells, see section, section, and section.

Create tables
Mark the position in the RichText field where you want to insert a table.
To create a table,

either click  and  insert a table in the tool bar,
choose Format | Table | Insert table from the menu bar,
or choose Table | Insert table from the context menu.
3#
Enter the numbers of columns and rows in the dialog box.


Click [Ok] to confirm.



