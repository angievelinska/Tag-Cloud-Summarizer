

A customized and configured CAE Feeder starts with the following command:

bin/cm caefeeder start

The following command stops a started CAE Feeder:

bin/cm caefeeder stop

Note, that you need to install the application as Windows Service if running on Windows. See Section "Starting the Components" for a description how to start CoreMedia components.